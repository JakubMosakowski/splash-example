package com.mosjak.snackbar.presentation.main.shared

import androidx.lifecycle.ViewModel

/**
 * Shared view model to use inside multiple fragments (inside one activity).
  */
abstract class SharedViewModel: ViewModel()
